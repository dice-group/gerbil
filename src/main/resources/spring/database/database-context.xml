<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:p="http://www.springframework.org/schema/p" xmlns:context="http://www.springframework.org/schema/context"
    xmlns:c="http://www.springframework.org/schema/c" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
    xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Database Properties -->
    <context:property-placeholder location="classpath:config/database.properties" />

    <bean id="experimentDAO" class="org.aksw.gerbil.database.ExperimentDAOImpl">
        <constructor-arg name="dataSource" ref="databaseDataSource" />
    </bean>

    <!-- Bean providing connection to database -->
    <!-- <bean id="databaseDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
        <property name="driverClass" value="${database.jdbc.driverClassName}" />
        <property name="jdbcUrl" value="${database.jdbc.url}" />
        <property name="user" value="${database.jdbc.username}" />
        <property name="password" value="${databse.jdbc.password}" />
        <property name="minPoolSize" value="${database.jdbc.minPoolSize}" />
        <property name="maxPoolSize" value="${database.jdbc.maxPoolSize}" />
        <property name="maxIdleTime" value="${database.jdbc.maxIdleTime}" />
    </bean> -->

    <!-- Test DataSource using an in-memory Database H2
    <beans profile="test"> -->

        <bean id="databaseDataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
            <property name="driverClassName" value="org.h2.Driver" />
            <property name="url" value="jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1;MODE=MySQL;" />
        </bean>
        <jdbc:initialize-database data-source="databaseDataSource">

            <jdbc:script location="classpath:mock/schema/experiment-schema.sql" />
            <jdbc:script location="classpath:mock/data/experiment-insert-test.sql" />
        </jdbc:initialize-database>

    <!-- </beans>  -->

</beans>
