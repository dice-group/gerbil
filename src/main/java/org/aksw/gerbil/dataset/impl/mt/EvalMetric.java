package org.aksw.gerbil.dataset.impl.mt;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class EvalMetric {


        public static void main(String[] args) throws Exception{
             /*   Process p = Runtime.getRuntime().exec("python3 src/main/java/org/aksw/gerbil/dataset/impl/mt/pyt/eval.py -R gerbil_data/datasets/data" +
                        "/en/references/reference1 " +
                        "-H gerbil_data/datasets/data/en/hypothesis -nr 1 -m bleu,meteor,chrf++,ter");
                BufferedReader reader = new BufferedReader(new InputStreamReader(p.getInputStream()));
                String line = "";
                while ((line = reader.readLine()) != null) {
                        System.out.println(line + "\n");
                }

              */
        }
        }

